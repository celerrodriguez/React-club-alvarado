import React from 'react' //ya no se usa pero se puede dejar
import '.Button/button.css'
const button = (props) => {
  return (
    <div onClick={} className='btn'>{text}</div>
  )
}

export default button
